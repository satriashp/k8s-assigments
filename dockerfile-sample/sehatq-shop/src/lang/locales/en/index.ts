import validation from './validation.json';
import translation from './translation.json';
import login from './login.json';
import quotation_list from './quotation_list.json';
import quotation_detail from './quotation_detail.json';

const en = {
  translation,
  validation,
  login,
  quotation_list,
  quotation_detail,
};

export default en;
